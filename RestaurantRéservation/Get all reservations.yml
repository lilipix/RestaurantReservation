info:
  name: Get all reservations
  type: http
  seq: 13

http:
  method: GET
  url: http://localhost:5000/api/restaurants/reservations
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
